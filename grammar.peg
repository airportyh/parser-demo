start = term

term
    = lhs:factor _ op:[+-/*] _ rhs:term { return { lhs, op, rhs } }
    / factor

factor
    = lhs:number _ op:[/*] _ rhs:factor { return { lhs, op, rhs } }
    / number

_ = [ ]*

number = digits:[0-9]+ { return Number(digits.join("")) }